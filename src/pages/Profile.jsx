import React from 'react';
import Box from "@mui/material/Box";

const Profile = () => {

    return (
        <Box
        sx={{
            mx: "auto",
            display: "flex",
            flexDirection: "column",
            alignItems: "center",
            marginTop: "10%",
          }}
        noValidate
        autoComplete="off"
      >
      </Box>
    );
};

export default Profile;